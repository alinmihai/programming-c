#include <stdio.h>
#include <stdlib.h>

#define MAX 50

void pol(){
    for (int i=0; i < MAX; i ++)

}

bool pregunta(

int main (int argc, char *argv[]){

    double pol[MAX];
    double x;

    for (int i=0; i<MAX; i++)
        scanf(" %i", pol[i]);

    printf("Dale un valor a la X: ");
    scanf(" %lf", &x);

	return EXIT_SUCCESS;
}
